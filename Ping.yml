---
- name: Test connection to my servers
  hosts: k8s_alt_servers
  # connection: local # ansible-playbook Ping.yml -b -K

  tasks:
    - name: Ping my servers
      become: true
      ansible.builtin.ping:
